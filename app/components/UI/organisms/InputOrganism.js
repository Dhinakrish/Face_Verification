import React from 'react';
import {SafeAreaView, StyleSheet} from 'react-native';
const InputOrganism = ({label}) => {
  return <SafeAreaView />;
};

const styles = StyleSheet.create({});

export default InputOrganism;
