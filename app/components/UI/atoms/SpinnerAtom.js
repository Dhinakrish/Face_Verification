import React from 'react';
import Spinner from 'react-native-spinkit';

const SpinnerAtom = () => {
  return <Spinner isVisible={true} size={12} type={'Wave'} color={'white'} />;
};

export default SpinnerAtom;
